<UserControl x:Class="Auditor.Agent.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Border Width="400" VerticalAlignment="Center" HorizontalAlignment="Center"
                Background="{StaticResource BgSurface2Brush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="16" Padding="40,36">
            <Border.Effect>
                <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.45" Color="#000000" />
            </Border.Effect>
            <StackPanel>
                <!-- Brand -->
                <Border Width="60" Height="60" CornerRadius="16" Background="{StaticResource Primary}"
                        HorizontalAlignment="Center">
                    <TextBlock Text="&#xE83D;" FontFamily="Segoe MDL2 Assets" FontSize="30"
                               Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <TextBlock Text="Auditor Agent" FontFamily="{StaticResource FontDisplay}" FontSize="22"
                           FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"
                           HorizontalAlignment="Center" Margin="0,16,0,2" />
                <TextBlock Text="Lokal akkaunt bilan kiring" Style="{StaticResource Meta}"
                           HorizontalAlignment="Center" Margin="0,0,0,26" />

                <TextBlock Text="LOGIN" Style="{StaticResource FieldLabel}" />
                <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16" />

                <TextBlock Text="PAROL" Style="{StaticResource FieldLabel}" />
                <PasswordBox x:Name="Pwd" PasswordChanged="Pwd_PasswordChanged" Margin="0,0,0,16" />

                <TextBlock Text="{Binding Error}" Foreground="{StaticResource Danger}" FontSize="12.5"
                           TextWrapping="Wrap" Visibility="{Binding Error, Converter={StaticResource StringToVis}}"
                           Margin="0,0,0,12" />

                <Button Content="Kirish" Style="{StaticResource BtnPrimary}" FontSize="14" Padding="14,11"
                        Command="{Binding SignInCommand}" IsEnabled="{Binding Busy, Converter={StaticResource NotBool}}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
